# 自检统一为错解自检：错解必须被数据击败

测点质量的判据中，「数据能把错解/暴力从正确解里区分出去」比「答案与标程逐点一致」更关键——标程 `std.cpp` 的正确性由出题人保证，而数据强度必须被主动验证。故 skill 的自检统一为**错解自检**：`wrong/` 里的每个解法（含假算法与暴力）都必须被至少一个测点击败，否则自动加固数据。其中「暴力」是特例——它正确但慢，靠大数据 TLE 击败而非 WA。这与直觉「暴力用于对拍验证正确性」相反，故单独记录。
